# Azure Landing Zone — Study Notes

Generated by the `azure-landing-zone-explorer` skill. All notes in English.
Scope: 33 repositories — see `references/alz-repos.md` (skill) for the authoritative catalog.

## Progress

| # | Repository | Category | Flavor | Modules Done | Total | Status |
|--:|------------|----------|--------|-------------:|------:|--------|
| A1 | Azure/ALZ-Bicep | Bicep deploy | Bicep | 8 | 8 | ✅ Complete |
| A2 | Azure/bicep-lz-vending | Bicep vending | Bicep | 2 | 2 | ✅ Complete |
| A3 | Azure/alz-bicep-accelerator | Bicep accelerator | Bicep + CI/CD | 5 | 5 | ✅ Complete |
| A4 | Azure/Azure-Landing-Zones | Portal/umbrella | Mixed | 3 | 3 | ✅ Complete |
| B1 | Azure/terraform-azurerm-avm-ptn-alz | TF AVM ptn (governance) | Terraform | 1 | 1 | ✅ Complete |
| B2 | Azure/terraform-azurerm-avm-ptn-alz-management | TF AVM ptn (management) | Terraform | 1 | 1 | ✅ Complete |
| B3 | Azure/terraform-azurerm-avm-ptn-alz-connectivity-hub-and-spoke-vnet | TF AVM ptn (hub-spoke) | Terraform | 1 | 1 | ✅ Complete |
| B4 | Azure/terraform-azurerm-avm-ptn-alz-connectivity-virtual-wan | TF AVM ptn (vWAN) | Terraform | 1 | 1 | ✅ Complete |
| B5 | Azure/terraform-azurerm-avm-ptn-virtualwan | TF AVM ptn (vWAN core) | Terraform | 2 | 2 | ✅ Complete |
| B6 | Azure/terraform-azure-avm-ptn-alz-sub-vending | TF AVM ptn (sub vending) | Terraform | 2 | 2 | ✅ Complete |
| B7 | Azure/terraform-azure-avm-ptn-alz-application-landing-zone-cicd-bootstrap-azure-devops | TF AVM ptn (applz CI/CD ADO) | Terraform | 1 | 1 | ✅ Complete (scaffold) |
| B8 | Azure/terraform-azure-avm-ptn-alz-application-landing-zone-cicd-bootstrap-github | TF AVM ptn (applz CI/CD GH) | Terraform | 1 | 1 | ✅ Complete (scaffold) |
| B9 | Azure/terraform-azurerm-vnet-gateway | TF module (gateway) | Terraform | 1 | 1 | ✅ Complete (obsolete) |
| C1 | Azure/terraform-azurerm-lz-vending | TF vending core | Terraform | 2 | 2 | ✅ Complete |
| D1 | Azure/terraform-azurerm-caf-enterprise-scale | Classic TF (archetype engine) | Terraform | 3 | 3 | ✅ Complete |
| E1 | Azure/Enterprise-Scale | ARM / ref arch | ARM | 2 | 2 | ✅ Complete |
| F1 | Azure/alz-terraform-accelerator | TF accelerator | Terraform + CI/CD | 1 | 1 | ✅ Complete |
| F2 | Azure/accelerator-bootstrap-modules | Bootstrap modules | Terraform | 2 | 2 | ✅ Complete |
| **F3** | **Azure/ALZ-PowerShell-Module** | **Accelerator driver** | **PowerShell** | **7** | **7** | ✅ **Complete** |
| G1 | Azure/Azure-Landing-Zones-Library | Library/engine | Data (JSON/YAML) | 2 | 2 | ✅ Complete |
| G2 | Azure/alzlib | Library/engine | Go | 3 | 3 | ✅ Complete |
| G3 | Azure/terraform-provider-alz | Provider | Go | 2 | 2 | ✅ Complete |
| G4 | Azure/arm-template-parser | Parser lib | C# (.NET) | 2 | 2 | ✅ Complete |
| H1 | Azure/terraform-state-importer | Migration tool | Go/Tool | 4 | 4 | ✅ Complete |
| I1 | Azure/sovereign-landing-zone | Specialized LZ | Bicep + PowerShell | 4 | 4 | ✅ Complete |
| J1 | Azure/Azure-Governance-Visualizer | Governance viz | PowerShell | 4 | 4 | ✅ Complete |
| J2 | Azure/Azure-Governance-Visualizer-Accelerator | Viz accelerator | CI/CD | 4 | 4 | ✅ Complete |
| J3 | Azure/alzarchitectureeditor | Architecture editor | Web app | 3 | 3 | ✅ Complete |
| J4 | Azure/alz-copilot | Copilot tool | Private | 0 | ? | 🔒 Blocked (private) |
| K1 | Azure-Samples/alz-terraform-sub-vending-demo-with-terraform-cloud-and-github | Sample (sub vending) | Terraform | 2 | 2 | ✅ Complete |
| K2 | Azure-Samples/...-persona-template-01 | Sample template | Template | 1 | 1 | ✅ Complete |
| L1 | CSU-SolEng-ALZ-Wiki (Azure DevOps) | Internal wiki | Wiki/Docs | 0 | ? | 🔒 Blocked (private) |
| L2 | TechStrategy-Infra-ALZPwsh-CodeSign (Azure DevOps) | Code sign pipeline | Pipeline/PS | 0 | ? | 🔒 Blocked (private) |

> Total: 33 repositories. Categories follow `references/alz-repos.md` groups A–L.
> **Status: 30 / 33 analyzed ✅** — every public repo across groups A–K is complete. The remaining **3 are
> blocked (private)**: J4 `alz-copilot`, L1 `CSU-SolEng-ALZ-Wiki`, L2 `TechStrategy-Infra-ALZPwsh-CodeSign`.
> Notes on scaffold/obsolete repos: B7/B8 are not-yet-implemented AVM scaffolds; B9 is obsolete (superseded by
> `terraform-azurerm-avm-ptn-vnetgateway`).

## Repository notes

### F3 · Azure/ALZ-PowerShell-Module (this workspace)

The `ALZ` PowerShell module — the **driver/orchestrator** of the ALZ Accelerator. It downloads
bootstrap + starter modules, collects user inputs, renders configuration files, and runs Terraform
to bootstrap a customer's Version Control System (GitHub / Azure DevOps) and Azure environment.

- [_overview.md](./ALZ-PowerShell-Module/_overview.md) — module structure, exported cmdlets, end-to-end flow + diagrams
- [module-Deploy-Accelerator.md](./ALZ-PowerShell-Module/module-Deploy-Accelerator.md) — the primary entry-point cmdlet and its orchestration flow
- [module-New-Bootstrap.md](./ALZ-PowerShell-Module/module-New-Bootstrap.md) — the core bootstrap engine (config rendering → Terraform)
- [module-Test-AcceleratorRequirement.md](./ALZ-PowerShell-Module/module-Test-AcceleratorRequirement.md) — pre-flight environment/connectivity checks
- [module-Grant-SubscriptionCreatorRole.md](./ALZ-PowerShell-Module/module-Grant-SubscriptionCreatorRole.md) — billing `SubscriptionCreator` role grant (EA/MCA)
- [module-New-AcceleratorFolderStructure.md](./ALZ-PowerShell-Module/module-New-AcceleratorFolderStructure.md) — scaffolds the local config folder from accelerator repos
- [module-Remove-Cleanup-Cmdlets.md](./ALZ-PowerShell-Module/module-Remove-Cleanup-Cmdlets.md) — the 3 destructive teardown cmdlets (Azure / GitHub / Azure DevOps)

**Done (7/7 exported cmdlets):** `Deploy-Accelerator` (+ `New-Bootstrap` engine), `Test-AcceleratorRequirement`,
`Grant-SubscriptionCreatorRole`, `New-AcceleratorFolderStructure`, `Remove-PlatformLandingZone`,
`Remove-GitHubAccelerator`, `Remove-AzureDevOpsAccelerator`. Repo coverage complete.

### F2 · Azure/accelerator-bootstrap-modules (remote)

The shared **bootstrap** Terraform that the F3 PowerShell module downloads and applies. Stands up the
VCS (GitHub / Azure DevOps) + Azure backend (state storage, OIDC identity, RBAC, management group) and
seeds the chosen starter module. Resolves the F3 `TODO: verify` on the bootstrap config schema.

- [_overview.md](./accelerator-bootstrap-modules/_overview.md) — purpose, `.config/ALZ-Powershell.config.json` mapping, structure, architecture + flow diagrams, repo relationships
- [module-bootstrap-root-modules.md](./accelerator-bootstrap-modules/module-bootstrap-root-modules.md) — the 3 root modules (`alz/azuredevops`, `alz/github`, `alz/local`)
- [module-shared-modules.md](./accelerator-bootstrap-modules/module-shared-modules.md) — the 6 `modules/*` building blocks (azure, github, azure_devops, files, file_manipulation, resource_names)

**Done (2/2 module groups):** root modules + shared modules. Config contract with F3 documented.

### F1 · Azure/alz-terraform-accelerator (remote)

The Terraform **starter** the F2 bootstrap seeds — the repo that actually deploys the ALZ **platform**
by orchestrating the B-series AVM pattern modules. Resolves the F3 `New-AcceleratorFolderStructure`
scenarios TODO (scenarios live in `examples/`).

- [_overview.md](./alz-terraform-accelerator/_overview.md) — starter config, templates structure, scenarios, AVM composition + flow diagrams, repo relationships
- [module-platform-landing-zone.md](./alz-terraform-accelerator/module-platform-landing-zone.md) — the `platform_landing_zone` starter module: config-templating + the 4 AVM ptn modules, provider aliases, versions

**Done (1/1 starter module groups):** `platform_landing_zone` (the meaningful starter; `empty`/`test` are trivial).
Links the chain to B1 `avm-ptn-alz`, B2 `avm-ptn-alz-management`, B3 hub-spoke, B4 virtual-wan.

### B1 · Azure/terraform-azurerm-avm-ptn-alz (remote)

The **governance core** AVM pattern module (registry `Azure/avm-ptn-alz/azurerm`, v0.21.0) called by F1 as
`module.management_groups`. Deploys the management-group hierarchy + Azure Policy + RBAC from an
**architecture/archetype** resolved by the `alz` provider — the junction to the data/engine layer (G1/G2/G3).

- [_overview.md](./avm-ptn-alz/_overview.md) — purpose, providers, the `alz_architecture` data-source mechanism, default MG tree, resources, inputs/outputs, chain to G1/G2/G3
- [module-avm-ptn-alz.md](./avm-ptn-alz/module-avm-ptn-alz.md) — data-source → locals → azapi resource flow, ordering/`time_sleep` model, the no-`depends_on` + `_dependencies` constraint

**Done (1/1 module groups):** the root module. Connects to G3 `terraform-provider-alz` → G2 `alzlib` → G1 `Azure-Landing-Zones-Library`.

### B2 · Azure/terraform-azurerm-avm-ptn-alz-management (remote)

The **management-subscription resources** AVM pattern module (registry `Azure/avm-ptn-alz-management/azurerm`,
v0.9.0), called by F1 as `module.management_resources` on the `azurerm.management` provider alias. A flat
resource module (no `alz` provider) deploying Log Analytics + optional Automation/Sentinel + Azure Monitor
DCRs + the AMA identity. Its outputs feed B1's policy `policy_default_values`.

- [_overview.md](./avm-ptn-alz-management/_overview.md) — purpose, providers, resources, the 3 DCRs, greenfield/BYO logic, inputs/outputs, the B2→B1 policy linkage
- [module-avm-ptn-alz-management.md](./avm-ptn-alz-management/module-avm-ptn-alz-management.md) — resource graph, deployment flow, the locals greenfield/brownfield switch, outputs

**Done (1/1 module groups):** the root module. Pairs with B1 (governance) on the platform/management layer.

### B3 · Azure/terraform-azurerm-avm-ptn-alz-connectivity-hub-and-spoke-vnet (remote)

The **hub-and-spoke connectivity** AVM pattern module (registry `Azure/avm-ptn-alz-connectivity-hub-and-spoke-vnet/azurerm`,
v0.17.1), called by F1 as `module.hub_and_spoke_vnet` on the `azurerm.connectivity` alias (xor B4 vWAN). A
composition module wiring ~13 AVM res modules + 2 local sub-modules to build hub VNet(s), Azure Firewall,
gateways, Bastion, private DNS, NAT and DDoS, driven by one `hub_virtual_networks` map.

- [_overview.md](./avm-ptn-alz-connectivity-hub-and-spoke-vnet/_overview.md) — purpose, providers, full module composition (top-level + mesh sub-module), architecture + multi-hub mesh diagrams, inputs/outputs
- [module-avm-ptn-alz-connectivity-hub-and-spoke-vnet.md](./avm-ptn-alz-connectivity-hub-and-spoke-vnet/module-avm-ptn-alz-connectivity-hub-and-spoke-vnet.md) — data flow from the hub map, the mesh sub-module (firewall + routing), deployment flow

**Done (1/1 module groups):** the root + mesh sub-module. Connectivity sibling of B4 (Virtual WAN).

### B4 · Azure/terraform-azurerm-avm-ptn-alz-connectivity-virtual-wan (remote)

The **Virtual WAN connectivity** AVM pattern module (registry `Azure/avm-ptn-alz-connectivity-virtual-wan/azurerm`,
F1 pins v0.15.0), called by F1 as `module.virtual_wan` on the `azurerm.connectivity` alias (xor B3 hub-spoke).
A composition module wiring a rich local `./modules/virtual-wan` sub-module (vWAN + Virtual Hubs + Secured-Hub
firewall + gateways + routing intent) plus AVM res modules + a sidecar VNet for Bastion/DNS.

- [_overview.md](./avm-ptn-alz-connectivity-virtual-wan/_overview.md) — purpose, B3-vs-B4 comparison, full module composition, architecture + multi-region diagrams, inputs/outputs
- [module-avm-ptn-alz-connectivity-virtual-wan.md](./avm-ptn-alz-connectivity-virtual-wan/module-avm-ptn-alz-connectivity-virtual-wan.md) — data flow from the hub map, the virtual-wan sub-module (Secured Hub + routing intent), deployment flow

**Done (1/1 module groups):** the root + virtual-wan sub-module. Connectivity sibling of B3 (hub-and-spoke). **B-series 4/4 complete.**

### G1 · Azure/Azure-Landing-Zones-Library (remote)

The **ALZ Library** — the authoritative, versioned **data** source (JSON/YAML, not IaC) of ALZ policy/role
assets, archetypes, architectures, and policy default values. Read by the `alz` provider (G3) via `alzlib`
(G2) and ultimately consumed by **B1 `avm-ptn-alz`**. Resolves B1's open question — archetype→policy mapping
lives here.

- [_overview.md](./Azure-Landing-Zones-Library/_overview.md) — purpose, repo + `platform/alz` structure, asset-type taxonomy, archetype→architecture→MG model, the Policy Default Values ↔ B2 linkage, the 3 platform libraries (alz/amba/slz), consumption chain + diagrams
- [module-platform-alz.md](./Azure-Landing-Zones-Library/module-platform-alz.md) — construct file formats (archetype / override / architecture / policy default values), the resource-definition assets, dependency + deployment diagrams

**Done (2/2 module groups):** the asset taxonomy + the `platform/alz` library deep-dive. Bottom of the
governance chain F1 → B1 → G3 → G2 → **G1**. Asset totals: 149 policy defs, 42 set defs, 80 assignments, 5 roles.

### G2 · Azure/alzlib (remote)

The **engine** (Go library) that reads the ALZ Library (G1), validates it, fetches built-in policy
definitions from Azure, and builds the deploy-ready model that **G3 `terraform-provider-alz`** exposes to
B1. Two layers: the `alzlib` **library model** and the `deployment` **Hierarchy** (scope-resolved MGs).
Resolves B1's open question on how archetypes/policy assets are turned into scoped resources.

- [_overview.md](./alzlib/_overview.md) — purpose, package map, the two-layer model, the `Init` load pipeline + `FromArchitecture` deployment pipeline, env config, resource-ID formats, consumption chain + diagrams
- [module-library-model.md](./alzlib/module-library-model.md) — the `alzlib` root package + `assets` + `processor`: `AlzLib` struct, query API, `Init` steps, versioned policy assets, built-in fetch
- [module-deployment-hierarchy.md](./alzlib/module-deployment-hierarchy.md) — the `deployment` package: `Hierarchy`/`HierarchyManagementGroup`, scope ID rewriting, `PolicyRoleAssignment` generation, the `AddDefaultPolicyAssignmentValue` G1↔B2 injection point

**Done (3/3 module groups):** overview + library model + deployment model. Next engine layer up: G3 `terraform-provider-alz`.

### G3 · Azure/terraform-provider-alz (remote)

The **`alz` Terraform provider** — a **data-source-only** provider (Terraform Plugin Framework) that runs
`alzlib` (G2) inside Terraform and exposes the resolved ALZ model. Its `alz_architecture` data source is
exactly what **B1 `avm-ptn-alz`** reads and flattens into `azapi_resource`s. Completes the data/engine
chain G1 → G2 → **G3** → B1.

- [_overview.md](./terraform-provider-alz/_overview.md) — purpose, repo structure, the `Configure` lifecycle (provider → alzlib.Init), provider schema, the two data sources, consumption chain + diagrams
- [module-alz-architecture-datasource.md](./terraform-provider-alz/module-alz-architecture-datasource.md) — the `alz_architecture` data source: inputs/outputs schema, the `Read` flow (FromArchitecture → overrides → PolicyRoleAssignments → JSON serialize), the three override knobs, gotchas

**Done (2/2 module groups):** provider overview + the `alz_architecture` data source (`alz_metadata` covered in the overview). Data/engine chain fully traced end to end.

### G4 · Azure/arm-template-parser (remote)

A **C# / .NET 7** tool (not Go — catalog correction) that parses ARM templates **offline** using the real
Azure Deployments engine, extracting resolved resources (esp. policy assignments) as JSON. It's a
**build-time library-authoring tool** that feeds the ALZ Library (G1) — *upstream* of the runtime chain,
and **not** a dependency of alzlib (G2) as the catalog implies.

- [_overview.md](./arm-template-parser/_overview.md) — purpose, corrected role/chain position, repo structure, CLI parameters, dependencies (Azure.Deployments.* engine), the two catalog corrections, diagrams
- [module-ArmTemplateProcessor.md](./arm-template-parser/module-ArmTemplateProcessor.md) — the core C# class: public API, the parse→validate→evaluate→expand pipeline, copy-loop/nested-resource handling, error degradation, helpers

**Done (2/2 module groups):** overview + `ArmTemplateProcessor` (CLI covered in overview). **Group G complete (G1–G4).**

### D1 · Azure/terraform-azurerm-caf-enterprise-scale (remote)

The **classic CAF Enterprise-Scale** Terraform module — the monolithic, single-instance ALZ implementation
whose core is the **HCL-native archetype engine** (the conceptual ancestor of G1–G3 + B1). **Deprecated**
(archived 2026-08-01); migration target is AVM (B1–B4). Documents the data-generation submodule pattern and
the `archetype_config_overrides` policy-injection mechanism.

- [_overview.md](./terraform-caf-enterprise-scale/_overview.md) — purpose, deprecation, repo structure (root + 5 submodules + locals/resources split), MG hierarchy, data-gen pattern, inputs/resources/outputs, AVM-equivalents, diagrams
- [module-archetype-engine.md](./terraform-caf-enterprise-scale/module-archetype-engine.md) — the `modules/archetypes` core: library loading (`fileset`+`templatefile`+`jsondecode`), archetype resolution, parameter/enforcement merge, the 5 output data lists
- [module-platform-submodules.md](./terraform-caf-enterprise-scale/module-platform-submodules.md) — `management`/`connectivity`/`identity`/`role_assignments_for_policy`: data-gen + `archetype_config_overrides` policy injection + the cross-module merge

**Done (3/3 module groups):** archetype engine + platform submodules + root overview. Classic ancestor of the AVM/alz-provider stack.

### E1 · Azure/Enterprise-Scale (remote)

The **Azure Landing Zones (Enterprise-Scale) reference implementation** — the conceptual + ARM **authority**
for ALZ and the Azure Portal "Deploy to Azure" experience. Policy is authored in Bicep and generated to ARM
JSON; `eslzArm.json` is the single Tenant-root master template. The upstream source the modern G1 Library and
classic D1 module mirror.

- [_overview.md](./enterprise-scale-arm/_overview.md) — purpose, repo structure, the "one template to rule them all" model, canonical MG hierarchy, the CAF design areas, resources-in-scope, relationship to D1/G1/G4, diagrams
- [module-eslzArm.md](./enterprise-scale-arm/module-eslzArm.md) — the `eslzArm/` ARM templates: structure, Bicep→JSON policy generation, deployment sequencing, scope-escape + inner/outer eval (sub vending), the DINE/DENY/DENYACTION guardrail catalog

**Done (2/2 module groups):** repo/concepts overview + the eslzArm ARM templates. The conceptual root of the entire ALZ ecosystem.

### B5 · Azure/terraform-azurerm-avm-ptn-virtualwan (remote)

The **generic Virtual WAN building block** AVM pattern module — vWAN + Virtual Hubs + Secured-Hub firewall +
routing intent + S2S/P2S/ER gateways, driven by a central `virtual_hubs` map-of-objects. **Deprecated +
archived**; its code was **migrated into B4's `virtual-wan` submodule** — so B5 is the standalone ancestor of
the sub-module documented inside B4.

- [_overview.md](./avm-ptn-virtualwan/_overview.md) — purpose, deprecation/B4-migration, the map-of-objects model, the 8 local submodules, inputs/outputs/resources, diagrams
- [module-avm-ptn-virtualwan.md](./avm-ptn-virtualwan/module-avm-ptn-virtualwan.md) — the root composition: map→submodule→resource wiring, key-indirection, Secured Hub + routing intent flow

**Done (2/2 module groups):** overview + root composition. The vWAN core that B4 wraps.

### B6 · Azure/terraform-azure-avm-ptn-alz-sub-vending (remote)

The **subscription vending** AVM pattern module — instantiated once per landing zone to create a subscription
+ baseline (networking / RBAC / RP registration / budgets / UMI + OIDC) in a single **AzAPI** `terraform
apply`. The **demand side** of ALZ (per-workload subscriptions) and the AVM repackaging of the `lz-vending`
engine (C1).

- [_overview.md](./avm-ptn-alz-sub-vending/_overview.md) — purpose, the C1 relationship, capabilities, the 10 local submodules, inputs/outputs/resources, chain position, diagrams
- [module-avm-ptn-alz-sub-vending.md](./avm-ptn-alz-sub-vending/module-avm-ptn-alz-sub-vending.md) — the vending flow (single apply + wait), capability toggles, the `subscription` + `virtual-network` submodules, IPAM/mesh/hub/vWAN peering

**Done (2/2 module groups):** overview + root vending flow. The demand-side counterpart to the platform modules.

### C1 · Azure/terraform-azurerm-lz-vending (remote)

The **original subscription-vending engine** (AzAPI + time, single `terraform apply`). **Archived (Jun
2026)** — seamless migration to its AVM successor **B6**. Source-verified: `Microsoft.Subscription/aliases`
with `parent_id = "/"` scope escape, idempotent `uuidv5` role-assignment names, and AVM `res`/`utl` modules
composed inside. Resolves B6's open question (C1 = source engine; B6 = AVM repackage + IPAM).

- [_overview.md](./terraform-azurerm-lz-vending/_overview.md) — purpose, archive/B6 lineage, repo structure, the 9 submodules + their AzAPI resources, capabilities, vending-family lineage diagram
- [module-lz-vending.md](./terraform-azurerm-lz-vending/module-lz-vending.md) — the single-apply flow, root→submodule wiring, scope resolution (relative_scope / resource_group_key), source-verified submodule mechanics

**Done (2/2 module groups):** overview + root vending flow. The archived ancestor of B6; the Terraform productization of E1's ARM sub-vending.

### A2 · Azure/bicep-lz-vending (remote)

The **Bicep subscription-vending** implementation — a `managementGroup`-scoped two-tier orchestration
(`main.bicep` → `subResourceWrapper`) composing **CARML v0.6.0** modules. **Archived (Jun 2026)** → AVM Bicep
registry `avm/ptn/lz/sub-vending`. The Bicep peer of C1 `lz-vending`; resolves C1's A2-parity open question.

- [_overview.md](./bicep-lz-vending/_overview.md) — purpose, archive/AVM lineage, the cross-language vending family, repo structure (src/self + src/carml), capabilities, the single-VNet model, A2-vs-C1 differences, diagrams
- [module-main-bicep.md](./bicep-lz-vending/module-main-bicep.md) — source-verified: the two-tier orchestration, the CARML pipeline, hub auto-detection, role-assignment scope splitting, and the deployment-script RP-registration mechanism

**Done (2/2 module groups):** orchestration overview + the main.bicep/subResourceWrapper deep-dive. Completes the vending family (E1→A2/C1→AVM).

### A1 · Azure/ALZ-Bicep (remote)

The **modular Bicep main line** — the clearest end-to-end view of the "management groups → policy → RBAC →
logging → connectivity" backbone. One module per capability under `infra-as-code/bicep/modules/`, each deployed
separately (no top-level orchestrator — outputs are operator-wired). Now **"Classic"** (AVM Bicep is the default
starter); **archived 2027-02-16**. Source-verified across the platform + connectivity modules.

- [_overview.md](./ALZ-Bicep/_overview.md) — repo metadata, "Classic"/deprecation status, design philosophy (operator-wired, multi-scope, `mc-` sovereign, PID telemetry), full module inventory, MG-hierarchy + hub-spoke + vWAN + deployment-flow diagrams, ecosystem links
- [module-managementGroups.md](./ALZ-Bicep/module-managementGroups.md) — the tenant-scope MG hierarchy (L1–L3, conditional child-MG unions, scope-escape variant)
- [module-policy.md](./ALZ-Bicep/module-policy.md) — `customPolicyDefinitions` (library-driven `[for]` loops) + `alzDefaultPolicyAssignments` (per-MG assignment mapping, logging wiring, enforcement kill-switches)
- [module-rbac.md](./ALZ-Bicep/module-rbac.md) — 4 CAF custom roles + the `roleAssignment*` helper family (deterministic GUID names)
- [module-logging.md](./ALZ-Bicep/module-logging.md) — LAW + Sentinel + ChangeTracking + Automation + AMA (via AVM `ptn/alz/ama`)
- [module-hubNetworking.md](./ALZ-Bicep/module-hubNetworking.md) — Hub VNet + Azure Firewall + Bastion + VPN/ER gateways + DDoS + firewall-default UDR
- [module-vwanConnectivity.md](./ALZ-Bicep/module-vwanConnectivity.md) — Virtual WAN + multi-hub + routing-intent-vs-default-route + SharedGlobal/PerRegion firewall policy + sidecar VNet
- [module-spokeNetworking-subscriptionPlacement.md](./ALZ-Bicep/module-spokeNetworking-subscriptionPlacement.md) — spoke VNet + spoke→hub UDR; and MG↔subscription placement

**Done (8/8 core module groups):** managementGroups, policy, RBAC, logging, hubNetworking, vwanConnectivity, spokeNetworking + subscriptionPlacement. The Bicep counterpart of the AVM/Terraform governance line; vending is a separate repo (A2).

### A3 · Azure/alz-bicep-accelerator (remote)

The **AVM Bicep starter** ("Bicep Azure Verified Modules for Platform Landing Zone") consumed by the F3 driver
— the modern successor that **replaced ALZ-Bicep Classic (A1)** in the Accelerator. It composes
`br/public:avm/...` modules (governance → `avm/ptn/alz/empty`; logging → `avm/res/*` + `avm/ptn/alz/ama`;
networking → `avm/res/network/*`). The **Bicep analogue of F1**; seeded by F2, driven by F3 via a 18-deployment
manifest run through **deployment stacks**. Source-verified across the config contract + all three layers.

- [_overview.md](./alz-bicep-accelerator/_overview.md) — identity correction (AVM, not classic ALZ-Bicep), F-line placement, repo structure, the 18-deployment manifest, deployment-stacks model, how it composes AVM, diagrams
- [module-config-contract.md](./alz-bicep-accelerator/module-config-contract.md) — `.config/ALZ-Powershell.config.json`: inputs→env-vars, retain/strip, the ordered 18-deployment manifest + 10 groups, the `test` starter
- [module-governance.md](./alz-bicep-accelerator/module-governance.md) — per-MG modules loading vendored G1 ALZ Library data + customer merge → `avm/ptn/alz/empty`; cross-MG RBAC `main-rbac.bicep` (deployment-stack workaround)
- [module-logging.md](./alz-bicep-accelerator/module-logging.md) — AVM-composed RG + LAW (+ Sentinel) + Automation + `avm/ptn/alz/ama`
- [module-networking.md](./alz-bicep-accelerator/module-networking.md) — array-driven multi-region hub from `avm/res/network/*` (+ DNS Private Resolver); vWAN variant

**Done (5/5 module groups):** config contract, governance, logging, networking (+ overview). Corrects the catalog's "wraps ALZ-Bicep" description — A3 is the AVM Bicep starter that replaced A1.

### J1 · Azure/Azure-Governance-Visualizer (remote)

**AzGovViz** — a ~20k-line PowerShell 7 script (the **read/audit side** of ALZ). It scans the tenant MG hierarchy
(Reader-only) through the **AzAPICall** module and renders HTML/CSV/MD/JSON governance reports. Two ALZ-specific
features bridge it to the deploy line: the **ALZ Policy Version Checker** (clones E1) and the **ALZ Policy
Assignments Checker** (clones G1). Engine/tooling analysis (data flow, not IaC). Source-verified.

- [_overview.md](./Azure-Governance-Visualizer/_overview.md) — purpose, repo + dev/prod build layout, end-to-end flow, the 4 outputs, the 2 ALZ checkers, ecosystem placement, diagrams
- [module-entry-and-flow.md](./Azure-Governance-Visualizer/module-entry-and-flow.md) — the entry script, ~80 parameters, verified execution order, `$azAPICallConf` + the central `$newTable` data model
- [module-data-collection.md](./Azure-Governance-Visualizer/module-data-collection.md) — discovery (getEntities/getSubscriptions/cacheBuiltIn) + the parallel `processDataCollection` + identity resolution via Graph
- [module-outputs-and-alz-checkers.md](./Azure-Governance-Visualizer/module-outputs-and-alz-checkers.md) — HTML (HierarchyMap/TenantSummary/DefinitionInsights/ScopeInsights) + CSV/MD/JSON + the two ALZ checkers in depth

**Done (4/4 module groups):** overview + entry/flow + data-collection + outputs/ALZ-checkers. The read side of ALZ; consumes E1 + G1 at runtime via `git clone`.

### H1 · Azure/terraform-state-importer (remote)

The ALZ **migration tool** — a Go (Cobra/Viper) CLI that imports brownfield Azure resources **into** Terraform.
It discovers resources (Resource Graph), runs `terraform plan`, maps Azure IDs ↔ planned TF resources, surfaces
conflicts as a reviewable CSV (3 issue types × 4 actions), and generates `import {}` blocks. Ships pre-built ALZ
configs (MG hierarchy, hub-spoke, vWAN). The classic-D1 → AVM-B-series migration path. Source-verified.

- [_overview.md](./terraform-state-importer/_overview.md) — purpose, two-phase/two-run workflow, Go packages, the pre-built ALZ configs, ecosystem placement (migrate FROM D1 → TO B1), diagrams
- [module-cli-and-config.md](./terraform-state-importer/module-cli-and-config.md) — the Cobra `run` command, global/run flags, the Viper YAML schema (queries, nameFormats, propertyMappings, meta.*), client composition
- [module-mapping-engine.md](./terraform-state-importer/module-mapping-engine.md) — `analyzer.MappingClient.Map()` correlating `azure.ResourceGraphClient` × `terraform.PlanClient`; analysis vs import mode
- [module-issues-and-outputs.md](./terraform-state-importer/module-issues-and-outputs.md) — the 3 issue types, 4 actions, the CSV columns + Replace handshake, and the output files (issues/imports/destroy/final)

**Done (4/4 module groups):** overview + CLI/config + mapping engine + issues/outputs. The brownfield→Terraform migration tool; pairs the classic D1 with the AVM B-series.

### I1 · Azure/sovereign-landing-zone (remote)

The **Sovereign Landing Zone (SLZ)** — the Microsoft Cloud for Sovereignty offering: a **PowerShell-orchestrated
Bicep** ALZ variant for regulatory/sovereignty compliance. It vendors **ALZ-Bicep (A1)** under `dependencies/` and
composes its logging + hub-networking + policy modules, then adds the sovereign overlay — two **confidential**
landing zones (Confidential Corp/Online, confidential-computing only) and the **Sovereignty Baseline policy
initiatives (SBPI)** enforcing data residency + CMK + confidential computing. One parameter file, one command
(`New-SovereignLandingZone.ps1`). ⚠️ Being archived H1 2026. Source-verified.

- [_overview.md](./sovereign-landing-zone/_overview.md) — identity (MCfS variant), the 4 landing zones, sovereignty pillars, repo structure, stage flow, relationship to ALZ-Bicep, diagrams
- [module-orchestration-and-stages.md](./sovereign-landing-zone/module-orchestration-and-stages.md) — `New-SovereignLandingZone.ps1` entry, the verified `all` flow, the per-stage scripts, bootstrap vs bootstrapScopeEscape
- [module-sovereign-platform.md](./sovereign-landing-zone/module-sovereign-platform.md) — `sovereignPlatform.bicep` composing vendored ALZ-Bicep (logging + hubNetworking + roleAssignments) + SLZ RGs + managed identity; verified outputs
- [module-compliance-and-policy.md](./sovereign-landing-zone/module-compliance-and-policy.md) — `policyInstallation` (ALZ defs + SBPI) + default/customCompliance assignments + exemption/remediation + the compliance dashboard

**Done (4/4 module groups):** overview + orchestration/stages + sovereign platform + compliance/policy. The Bicep sovereign overlay on A1 (ALZ-Bicep), layering E1's ALZ Policies + the SBPI.

### J2 · Azure/Azure-Governance-Visualizer-Accelerator (remote)

The **AzGovViz Accelerator** — a GitHub **template repository** that turns [AzGovViz (J1)](./Azure-Governance-Visualizer/_overview.md)
into a scheduled CI/CD job. Create a private copy, wire up two Entra identities (an **OIDC** scanning SP + a Web App
user-auth app), and four GitHub Actions workflows deploy a secure **Entra-gated Azure Web App** (Bicep), run AzGovViz
on a cron, publish the HTML report, and self-update from both AzGovViz and the accelerator's own releases. The only
Azure resource is the report-hosting Web App. Source-verified.

- [_overview.md](./Azure-Governance-Visualizer-Accelerator/_overview.md) — identity, the 6 workflows + script + Bicep, create-from-template onboarding (2 identities), data flow, J1 relationship, diagrams
- [module-deploy-workflows.md](./Azure-Governance-Visualizer-Accelerator/module-deploy-workflows.md) — `deployAzGovVizAccelerator` (Bicep deploy) + `deployAzGovViz` (run + publish), incl. the publish security guard (refuses unless Entra auth on)
- [module-sync-workflows.md](./Azure-Governance-Visualizer-Accelerator/module-sync-workflows.md) — `syncAzGovViz` (pull J1 `pwsh/`) + `syncAccelerator` (PR to `releases/`) + `Invoke-GitHubReleaseFetcher.ps1`
- [module-webapp-infra.md](./Azure-Governance-Visualizer-Accelerator/module-webapp-infra.md) — `webApp.bicep`: App Service Plan + Web App + `authsettingsV2` Easy Auth restricted to an authorized security group

**Done (4/4 module groups):** overview + deploy workflows + sync workflows + Web App infra. The CI/CD + hosting wrapper around J1 (AzGovViz).

### J3 · Azure/alzarchitectureeditor (remote)

The **ALZ Architecture Editor** — an **experimental**, framework-free browser app for visually authoring an ALZ
**architecture definition** (the management-group hierarchy + per-MG archetypes). Build/edit the tree (drag-drop
reparenting, archetype tag chips, greenfield/brownfield `exists` rules) and **export the JSON** that the
[ALZ Terraform provider (G3)](./terraform-provider-alz/_overview.md) consumes via the
[ALZ Library (G1)](./Azure-Landing-Zones-Library/_overview.md) schema. One `index.html` + `script.js` + `styles.css`,
no build, no backend, all client-side. Source-verified.

- [_overview.md](./alzarchitectureeditor/_overview.md) — purpose, file structure, the architecture-definition data model, the default ALZ hierarchy, ecosystem link to G1/G2/G3, diagrams
- [module-editor-app.md](./alzarchitectureeditor/module-editor-app.md) — `index.html` UI + `script.js` logic: state, load/new/upload/download, tree rendering, MG CRUD, field editing, archetype tags, drag-drop reparenting, validation, JSON preview
- [module-data-model-and-schema.md](./alzarchitectureeditor/module-data-model-and-schema.md) — `schema.json` (draft-07) + the sample default ALZ tree; the 5 MG fields, the `exists`/`parent_id` invariants, relationship to the G1 `architecture` construct

**Done (3/3 module groups):** overview + editor app + data model/schema. The visual authoring front-end for the G1 `architecture` construct (J1 is the read/audit side; J3 is authoring).

### A4 · Azure/Azure-Landing-Zones (remote)

The **ALZ documentation umbrella** — a **Hugo** static site (theme `hugo-geekdoc`) published to GitHub Pages at
**azure.github.io/Azure-Landing-Zones**. Not an IaC deploy repo: it's the central docs hub that frames the
“Azure landing zone Journey” (Bootstrap → Platform → Subscription vending → Application landing zones → Workloads)
and documents every implementation — [Accelerator (F1/F2/F3)](./alz-terraform-accelerator/_overview.md),
[Bicep (A1/A3)](./ALZ-Bicep/_overview.md), Terraform/AVM (B-series), the [Library (G1)](./Azure-Landing-Zones-Library/_overview.md),
sub-vending, policy, and the Portal Accelerator. It **embeds [J3](./alzarchitectureeditor/_overview.md)** as a git
submodule and ships repo-ops utilities (cost estimates, GitHub label/issue tooling, AI issue-triage). Source-verified.

- [_overview.md](./Azure-Landing-Zones/_overview.md) — umbrella role, the ALZ Journey, repo structure, content taxonomy, J3 submodule, Hugo build/publish, ecosystem hub diagram
- [module-docs-site-and-content.md](./Azure-Landing-Zones/module-docs-site-and-content.md) — the Hugo site (`docs/`), content sections (accelerator/bicep/terraform/sub-vending/policy/portal), the `.gitmodules` J3 embed, and `hugo.yml` build → GitHub Pages
- [module-utilities-and-automation.md](./Azure-Landing-Zones/module-utilities-and-automation.md) — `Get-ScenarioCostEstimates.ps1` (Azure Retail Prices), the github-labels scripts, and the `.github` agentic AI issue-triage automation

**Done (3/3 module groups):** overview + docs-site/content + utilities/automation. The documentation front-door for the whole ALZ product family (embeds J3, hosts the Portal Accelerator docs).

### B7 + B8 · Application landing zone CI/CD bootstrap (Azure DevOps / GitHub) (remote)

The intended **application** landing zone CI/CD bootstrap AVM pattern pair — B7 for **Azure DevOps**, B8 for **GitHub**
(the per-app analogue of the platform bootstrap in [F2 accelerator-bootstrap-modules](./accelerator-bootstrap-modules/_overview.md)).
⚠️ **Verified finding: both are scaffold-only — not yet implemented.** The two repos are **byte-for-byte identical**
AVM Terraform module templates (`main.tf` is the dummy `azurerm_resource_group.TODO`; required inputs are the generic
`location`/`name`/`resource_group_name`; provider set has **no `azuredevops`/`github` provider**). Documented honestly
as intended-role + verified-scaffold; the CI/CD resources are marked `TODO: verify` (not fabricated). Source-verified.

- [B7 _overview.md](./avm-ptn-alz-applz-cicd-bootstrap-azure-devops/_overview.md) — intended ADO bootstrap role, the verified template-scaffold state, AVM layout, the `.agents` build-out skill, ecosystem placement
- [B8 _overview.md](./avm-ptn-alz-applz-cicd-bootstrap-github/_overview.md) — intended GitHub bootstrap role, identical-scaffold note, intended `github_*`/OIDC resources, ecosystem placement

**Done (overview, scaffold state):** both documented as not-yet-implemented AVM scaffolds; revisit when the modules are built out.

### B9 · Azure/terraform-azurerm-vnet-gateway (remote)

The classic (pre-AVM) Terraform module that deploys an Azure **Virtual Network Gateway** (VPN or ExpressRoute) plus its
auxiliary resources — `GatewaySubnet`, public IP(s), optional route table, local network gateways, ExpressRoute circuit
peerings, and the gateway connections. ⚠️ **Obsolete — superseded by `terraform-azurerm-avm-ptn-vnetgateway`** (the AVM
version; pinned to old azurerm 3.x). Fully implemented (unlike B7/B8) and **source-verified** (`main.tf`, `locals.tf`,
terraform-docs README).

- [_overview.md](./terraform-azurerm-vnet-gateway/_overview.md) — identity + obsolete notice, features, repo layout, resource-topology + deployment-flow diagrams, ecosystem placement (AVM successor, B3 hub, A1 hubNetworking)
- [module-vnet-gateway.md](./terraform-azurerm-vnet-gateway/module-vnet-gateway.md) — full inputs (VPN/ER, LNGs, ER circuits, P2S, route table), the 8 resources + conditionals, the `locals.tf` ip-config/connection/peering wiring, the 6 curated outputs

**Done (1/1 module group):** overview + the single flat module. The classic VNG primitive; new work should use the AVM successor.

### K1 + K2 · Azure-Samples sub-vending demo (Terraform Cloud + GitHub) + persona template (remote)

A **self-service subscription vending sample**. K1 is the demo orchestrator: a `repository_dispatch` GitHub Action runs a
root Terraform module (in **Terraform Cloud**) that composes [C1 `lz-vending`](./terraform-azurerm-lz-vending/_overview.md)
(subscription + RGs + UMI + TFC federated creds) + a **TFC workspace** module + a **GitHub** module that creates the user's
repo from the **K2 persona template** and seeds it with the TFC token/org/workspace. The user then runs their repo's
pipeline to deploy an example **Windows VM** into the vended subscription. Conceptual demo (PAT + TFC user tokens).
Source-verified.

- [K1 _overview.md](./sample-sub-vending-tfc-github/_overview.md) — the self-service flow, what a vend produces, repo layout, the 3 composed modules, ecosystem placement (C1/B6/A2/F1), diagrams
- [K1 module-vend-flow.md](./sample-sub-vending-tfc-github/module-vend-flow.md) — `vend.yml` repository_dispatch → script → root `main.tf`: `lz_vending` (C1 v3.4.1) + `tfworkspace` (tfe_*) + `github` (repo-from-template + secrets), verified wiring
- [K2 _overview.md](./sample-sub-vending-persona-template-01/_overview.md) — the persona template: `terraform { cloud {} }` + VNet/subnet/NIC/Windows VM example, how K1 wires it (template + TF_API_TOKEN/TF_CLOUD_* ), OIDC auth

**Done (3/3 module groups):** K1 overview + vend-flow + K2 overview. The end-to-end self-service vending sample on top of C1.

## Last session
- Last target: `K1`/`K2` (`Azure-Samples/alz-terraform-sub-vending-demo-with-terraform-cloud-and-github` + `...-persona-template-01`) — **fully analyzed** remotely (source-verified `main.tf`, the github/tfworkspace sub-modules, `vend.yml`, and K2's `main.tf`). K1 is a **self-service subscription vending demo**: a `repository_dispatch` GitHub Action runs a Terraform Cloud root module that composes [C1 `lz-vending`](./terraform-azurerm-lz-vending/_overview.md) v3.4.1 + a TFC workspace + a GitHub repo created from the **K2 persona template**; the user then deploys an example Windows VM into the vended subscription. Conceptual demo (GitHub PAT + TFC user/team tokens, OIDC to Azure via UMI federated creds).
- Next suggested: all of groups A–K are now analyzed except the private repos. Remaining: `J4` (`alz-copilot`, private), `L1` (`CSU-SolEng-ALZ-Wiki`, Azure DevOps), `L2` (`TechStrategy-Infra-ALZPwsh-CodeSign`, Azure DevOps) — all **blocked (private)** until access is available.

## Glossary
See [glossary.md](./glossary.md).
